<?php
require_once __DIR__ . '/includes/functions.php';
$page_title = 'Services'; $services = db()->query("SELECT s.*, salon.name salon_name, salon.city FROM services s JOIN salons salon ON salon.id=s.salon_id WHERE s.status='active' AND salon.status='approved' ORDER BY s.name")->fetchAll(); include __DIR__ . '/includes/header.php';
?><header class="page-intro"><div class="container"><div class="section-label">The menu</div><h1 class="display-4">A little time, well spent.</h1><p class="muted col-lg-6">Explore services from our independent salon partners. Transparent pricing, thoughtful work.</p></div></header><main class="section-pad"><div class="container"><div class="row g-3"><?php foreach ($services as $service): ?><div class="col-md-6 col-lg-4"><div class="service-card p-4 h-100"><div class="d-flex justify-content-between"><span class="pill"><?= e($service['salon_name']) ?></span><span class="price"><?= money($service['price']) ?></span></div><h2 class="h4 mt-4"><?= e($service['name']) ?></h2><p class="muted small"><?= e($service['description']) ?></p><div class="small muted mt-4"><i class="bi bi-clock me-1"></i><?= e($service['duration_minutes']) ?> min · <?= e($service['city']) ?></div></div></div><?php endforeach; ?></div></div></main><?php include __DIR__ . '/includes/footer.php'; ?>
